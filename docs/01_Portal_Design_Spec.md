# Portal Design Specification

> **适用范围**: `iai-teaching-portal` (教师端 PC 入口)

## 1. 设计目标
- **专业感**：采用 "ReOpenInnoLab" 品牌视觉，体现教育科技的严谨与现代感。
- **高效率**：无需登录即可浏览工具列表（配置驱动），常用工具一键直达。
- **可扩展**：支持通过 JSON 配置动态添加新工具入口，无需修改代码。

## 2. UI 设计规范

### 色彩系统
- **主色 (Primary)**: `#3B82F6` (学术蓝) - 用于按钮、高亮链接。
- **文字 (Text)**: 
    - 标题 `#0F172A` (Slate-900)
    - 正文 `#475569` (Slate-600)
    - 辅助 `#94A3B8` (Slate-400)
- **背景 (Background)**: `#F8FAFC` (Slate-50) - 整体背景色，卡片使用纯白 `#FFFFFF`。

### 字体系统
- **品牌字体**: `Outfit` (Google Fonts) - 用于 "ReOpenInnoLab" 和数字。
- **正文字体**: 系统默认无衬线字体 (PingFang SC, Microsoft YaHei, etc.)。

### 布局结构
1.  **Header (顶部导航)**:
    - Logo + 品牌名 ("ReOpenInnoLab | 智教空间")。
    - 全局搜索框 (支持拼音/标签搜索)。
    - 用户头像与帮助入口。
2.  **Sidebar (侧边导航)**:
    - 可折叠式分组菜单 (教学流程、课堂工具、备课资源等)。
    - 使用 Iconify 图标增强识别度。
3.  **Main Content (主内容区)**:
    - **Banner**: 欢迎语与 AI 小技巧提示。
    - **Card Grid**: 响应式网格布局，展示应用卡片。

## 3. 核心交互

### 应用卡片 (Entry Card)
- **状态徽标**: 
    - 🟢 `Available`: 正常使用。
    - 🟡 `Maintenance`: 维护中，不可点击。
    - 🔵 `New`: 新上线功能。
- **点击行为**:
    - **内部应用**: 路由跳转 (如 `/apps/ocr`)。
    - **外部链接**: 新标签页打开，自动附带 `?source=portal` 参数。
- **收藏功能**: 点击卡片右上角 "星号" 可将应用固定到首页 "常用应用" 区 (Local Storage 持久化)。

### 全局搜索
- 实时过滤：输入关键词即时筛选卡片。
- 模糊匹配：支持匹配应用名称、描述和标签。

## 4. 配置文件结构 (`portalConfig.ts`)

Portal 的内容完全由 `src/data/portalConfig.ts` 驱动，数据结构如下：

```typescript
interface PortalConfig {
  title: string;        // 页面标题
  groups: string[];     // 分组列表
  entries: {
    id: string;
    name: string;       // 应用名称
    description: string;// 短描述
    tags: string[];     // 搜索标签
    url: string;        // 跳转链接
    status: 'available' | 'maintenance' | 'new';
    featured?: boolean; // 是否推荐
    group: string;      // 所属分组
  }[];
  announcements: {      // 公告栏
    id: string;
    title: string;
    content: string;
    pinned?: boolean;
  }[];
}
```

## 5. 开发指南

- **添加新应用**: 直接修改 `portalConfig.ts` 的 `entries` 数组。
- **修改主题**: 编辑 `tailwind.config.js` 或 `src/assets/styles/variables.css`。
- **构建部署**: `npm run build` 生成纯静态文件，可部署至 Nginx/Vercel。
